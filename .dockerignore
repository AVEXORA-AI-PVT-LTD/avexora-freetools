# Everything here is either rebuilt inside the image or must never enter it.
.git
node_modules
.next
out
coverage
samples

# Secrets. .env.example is the only env file that may be baked in.
.env
.env.*
!.env.example

*.tsbuildinfo
npm-debug.log*
.DS_Store
Dockerfile
.dockerignore
